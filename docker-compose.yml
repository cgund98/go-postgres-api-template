version: '3.8'

services:
  workspace:
    build:
      context: .
      dockerfile: resources/docker/workspace.Dockerfile
    platform: linux/amd64
    container_name: go-postgres-api-template-workspace
    volumes:
      - .:/workspace
      - go-mod-cache:/go/pkg/mod
    working_dir: /workspace
    ports: 
      - "8080:8080"
    stdin_open: true
    tty: true
    environment:
      - CGO_ENABLED=0
      - GOOS=linux
      - GOARCH=amd64
      - OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://aspire-dashboard:18890
      - OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://aspire-dashboard:18890/v1/traces
      - OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://aspire-dashboard:18890/v1/metrics
      - OTEL_EXPORTER_OTLP_LOGS_ENDPOINT=http://aspire-dashboard:18890/v1/logs
    depends_on:
      - postgres
      - localstack
      - aspire-dashboard

  postgres:
    image: postgres:16
    container_name: go-postgres-api-template-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: app
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  localstack:
    image: localstack/localstack:latest
    container_name: go-postgres-api-template-localstack
    ports:
      - "4566:4566"
      - "4510-4559:4510-4559"  # External services port range
    environment:
      - SERVICES=sns,sqs
      - DEBUG=1
      - PERSISTENCE=0
      - LAMBDA_EXECUTOR=local
      - DOCKER_HOST=unix:///var/run/docker.sock
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4566/_localstack/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  aspire-dashboard:
    image: mcr.microsoft.com/dotnet/aspire-dashboard:latest
    container_name: go-postgres-api-template-aspire-dashboard
    ports:
      - "18888:18888"
      - "4317:18889"
      - "4318:18890"
    environment:
      - DOTNET_DASHBOARD_UNSECURED_ALLOW_ANONYMOUS='true'

volumes:
  postgres_data:
  go-mod-cache:
